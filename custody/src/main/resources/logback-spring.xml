<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="custody"/>

    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timeZone>UTC</timeZone>
            <customFields>{"service":"${appName}","log_format":"json"}</customFields>
            <!-- Mask private keys (64 hex) and signed raw transactions (>64 hex) in all log values -->
            <jsonGeneratorDecorator class="net.logstash.logback.mask.MaskingJsonGeneratorDecorator">
                <valueMask>0x[a-fA-F0-9]{64,}</valueMask>
            </jsonGeneratorDecorator>
        </encoder>
    </appender>

    <appender name="JSON_PRETTY_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/custody-pretty.json.log</file>
        <append>false</append>
        <filter class="lab.custody.common.logging.RequireCorrelationIdFilter"/>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timeZone>UTC</timeZone>
            <customFields>{"service":"${appName}","log_format":"json_pretty"}</customFields>
            <jsonGeneratorDecorator class="net.logstash.logback.decorate.CompositeJsonGeneratorDecorator">
                <jsonGeneratorDecorator class="net.logstash.logback.decorate.PrettyPrintingJsonGeneratorDecorator"/>
                <jsonGeneratorDecorator class="net.logstash.logback.mask.MaskingJsonGeneratorDecorator">
                    <valueMask>0x[a-fA-F0-9]{64,}</valueMask>
                </jsonGeneratorDecorator>
            </jsonGeneratorDecorator>
        </encoder>
    </appender>

    <!-- 8-1-5: production 프로파일 — JSON_CONSOLE만 사용 (파일 appender 제외) -->
    <springProfile name="production">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

    <!-- non-production: JSON_CONSOLE + lab 패키지는 JSON_PRETTY_FILE 병행 출력 -->
    <springProfile name="!production">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>

        <!-- Pretty file log is for lab request-flow logs only (exclude framework/system/test noise). -->
        <logger name="lab.custody.common" level="INFO" additivity="true">
            <appender-ref ref="JSON_PRETTY_FILE"/>
        </logger>
        <logger name="lab.custody.orchestration" level="INFO" additivity="true">
            <appender-ref ref="JSON_PRETTY_FILE"/>
        </logger>
        <logger name="lab.custody.adapter" level="INFO" additivity="true">
            <appender-ref ref="JSON_PRETTY_FILE"/>
        </logger>
    </springProfile>
</configuration>
